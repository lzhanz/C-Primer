cout<<"字符串容量为:"<<word.size()<<endl;

	for(decltype(word.size()) x=0;x<word.size();++x)
	{
		auto &a =word[x];
		for(int b=0;b<a.length();++b)
		{
			if(islower(a[b]))
			{
				a[b]-=32;
			}
		}
	}
	for(decltype(word.size()) x=0;x<word.size();++x)
	{
	  cout<<word[x]<<endl;
	}
